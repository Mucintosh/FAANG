let styles = ['rap','rnb','jazz','blues','pop'];

if (styles.length % 2 != 0) {
  styles[Math.floor(count/2)] = 'classic';
  alert(styles);
} else {
  alert('No middle element, arrays has even number of elements');
};

styles.shift();
alert(styles);

styles.unshift('country','reggy');
alert(styles);


